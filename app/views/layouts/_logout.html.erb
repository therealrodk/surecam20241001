<% if user_signed_in? %>
  <div class="flex justify-end m-2">
    <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "px-4 py-2 mb-4 bg-red-200 hover:bg-blue-400 rounded-lg" %>
  </div>
<% end %>
